name: Keep Supabase Alive
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Edge Function + Database
        run: |
          curl -sf https://mbputwgppweoeujiszgv.supabase.co/functions/v1/bodulica-api/health || true
          curl -sf https://mbputwgppweoeujiszgv.supabase.co/functions/v1/bodulica-api/products || true
